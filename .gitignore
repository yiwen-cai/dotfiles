# ============================================================
# 敏感信息（绝对不能上传）
# ============================================================
# Claude
claude/settings.json
claude/config.json

# Codex
codex/auth.json
codex/.env
# 注意: codex/config.toml 不含密钥，正常上传

# Cursor
cursor/mcp.json
# 注意: cursor/settings.json、keybindings.json、hooks.json 不含密钥，正常上传

# 通用密钥/凭证
*.pem
*.key
*_id_rsa
id_ed25519
.env
!.env.example
secrets.zsh
!secrets.zsh.example
!.gitignore

# ============================================================
# 运行时数据 / 缓存 / 日志（本机生成，无需同步）
# ============================================================
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log
*.jsonl
*.bak
*.tmp
cache/
logs/
backups/
sessions/
history.*
stats-cache.json

# ============================================================
# 本地技能内的大体积参考仓库（skills-local/*/repos/）
# 不进 git；部署时 rsync 排除，本机已有 checkout 保留
# ============================================================
skills-local/*/repos/
